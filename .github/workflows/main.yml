# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
  #  Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This  workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: '8' # The JDK version to make available on the path.
          java-package: jdk # (jre, jdk, or jdk+fx) - defaults to jdk
          architecture: x64 # (x64 or x86) - defaults to x64

       # Runs a set of commands using the runners shell
      - name: Compile Titan Project
        run: |
          cd src
          javac    titan/*.java  -d ../bin
          javac   -cp ../lib/org.junit_4.13.0.v20200204-1500.jar:../lib/org.hamcrest.core_1.3.0.v20180420-1519.jar       titan/tests/*.java -d ../bin

      - name: Run Titan Test Suite 
        run: |
           cd bin
           java java -cp ../lib/org.junit_4.13.0.v20200204-1500.jar:../lib/org.hamcrest.core_1.3.0.v20180420-1519.jar:. titan/tests/TestSuiteRunner 

